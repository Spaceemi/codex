#include <bits/stdc++.h>

using namespace std;

int main() {
int a;
cin>>a;
vector<int>v;
for(int i=0; i<a; i++){
    int b;
    cin>>b;
    v.push_back(b);
}
int s;
for(int i=0; i<v.size(); i++){

    s+=v[i];
}
   cout<<s;
    return 0;
}
